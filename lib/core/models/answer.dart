class Answer {
  final String name;
  final double point;

  Answer({required this.name, required this.point});
  // factory Answer.fromJson(Map<String, dynamic> json) {
  //   return Answer(
  //     name: json["answer"],
  //     point: json["score"],
  //   );
  // }
}
